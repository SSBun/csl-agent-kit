# SOP Index

Use this index to choose the smallest relevant built-in SOP. Do not read SOP files by default.

Only read an SOP when the current task clearly matches its trigger:

| Trigger | SOP |
|---|---|
| Swift API design, public Swift interface changes, naming methods/properties/types, argument labels, documentation comments for Swift declarations | `skills/sop-creator/sops/swift-api-design.md` |
| Swift formatting, source file structure, imports, whitespace, Google Swift style, Swift programming practices | `skills/sop-creator/sops/swift-google-style.md` |

If no trigger clearly matches, do not load built-in SOP files.

If multiple SOPs match, read only the smallest relevant set and state which SOPs apply.

SOPs never override system, developer, explicit user, platform safety, repository, sandbox, or tool permission rules.

