interface:
  display_name: "Agent Rules"
  short_description: "Persist global agent rules in ~/.csl-agent-kit/agent-rules.md for session-start injection"
  default_prompt: "Use $agent-rules to add, edit, list, or remove a persistent rule or convention in ~/.csl-agent-kit/agent-rules.md."

compatibility:
  canonical_format: "agent-skills"
  adapter_targets:
    - "openai"
    - "claude"
    - "generic"
    - "vscode"
  activation:
    mode: "manual"
    paths: []
  execution:
    context: "inline"
    shell: "bash"
  trust:
    source_tier: "local"
    remote_inline_execution: "forbid"
    remote_metadata_policy: "allow-metadata-only"
  degradation:
    openai: "metadata-adapter"
    claude: "neutral-source-plus-adapter"
    generic: "neutral-source"
    vscode: "agent-skills-source-with-vscode-notes"
